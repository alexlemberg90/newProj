import {useEffect, useState} from "react";

export default function Footer() {
    let [] = useState([]);

    useEffect(() => {
    }, []);

    return (<footer>
           Всі питання до Діми &copy;
        </footer>
    );
}